import { switchActiveTopic } from "./logic";

// export function createTopicBtnListeners(){
//     let topicBtns = document.getElementsByClassName('topic');
//     topicBtns.array.forEach(element => element.addEventListner('click', function(e){
//         switchActiveTopic(e.target.dataset.value)
//     }))
// }

// for each topic in topics list
// add 'click' event listened, switchActiveTopic(index)